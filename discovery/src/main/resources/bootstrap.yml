#######
# LOCAL
#######

spring:
  profiles:
    - local
  application:
    name: discovery
  cloud:
    config:
      uri: http://localhost:8888

---

#######
# DEV
#######

spring:
  profiles:
    - dev
  application:
    name: discovery
  cloud:
    config:
      uri: http://localhost:8888
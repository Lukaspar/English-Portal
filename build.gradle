apply plugin: 'java'

subprojects {
    version = '1.0'
}

repositories {
    mavenCentral()
}

dependencies {
    compile project(":authentication")
    compile project(":configuration")
}

test {
    useJUnitPlatform()
}

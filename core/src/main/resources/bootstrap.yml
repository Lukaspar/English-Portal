#######
# LOCAL
#######

spring:
  profiles:
    - local
  application:
    name: core
  cloud:
    config:
      uri: http://localhost:8888

---

#######
# DEV
#######

spring:
  profiles:
    - dev
  application:
    name: core
  cloud:
    config:
      uri: http://localhost:8888